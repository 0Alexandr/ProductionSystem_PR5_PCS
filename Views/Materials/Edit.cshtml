@model ProductionSystem.Models.Material
@{
    ViewData["Title"] = "Редактировать материал";
}

<div class="breadcrumb">
    <a href="/Materials">Материалы</a>
    <span class="breadcrumb-sep">›</span>
    <span class="breadcrumb-current">Редактировать: @Model.Name</span>
</div>

<div style="max-width:600px;">
    <div class="card">
        <div class="card-header">
            <div class="card-title">Редактировать материал</div>
        </div>
        <div class="card-body">
            <form method="post" action="/Materials/Edit/@Model.Id">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <div class="form-grid">
                    <div class="form-group full">
                        <label class="form-label">Название *</label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="invalid-feedback"></span>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Количество</label>
                        <input asp-for="Quantity" class="form-control" type="number" step="0.001" min="0" />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Единица измерения</label>
                        <select asp-for="UnitOfMeasure" class="form-control">
                            <option>кг</option>
                            <option>шт</option>
                            <option>литр</option>
                            <option>м</option>
                            <option>м²</option>
                            <option>упак</option>
                        </select>
                    </div>
                    <div class="form-group full">
                        <label class="form-label">Минимальный запас</label>
                        <input asp-for="MinimalStock" class="form-control" type="number" step="0.001" min="0" />
                    </div>
                </div>
                <div style="display:flex;gap:10px;margin-top:20px;">
                    <button type="submit" class="btn btn-primary btn-lg">Сохранить</button>
                    <a href="/Materials" class="btn btn-outline btn-lg">Отмена</a>
                </div>
            </form>
        </div>
    </div>
</div>
